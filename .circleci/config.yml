version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10
    branches:
      only:
        - master
    steps:
      - add_ssh_keys:
          fingerprints:
            - "cc:ed:d9:34:e7:80:1a:68:2d:93:25:d4:5e:ec:f5:f9"
      - checkout
      restore_cache:
        keys:
          - dependencies
      - run:
          name: Install
          command: yarn install
      - save_cache:
        key: dependencies
        paths:
          - node_modules
      - run:
        name: build
        command: yarn build
      - run:
          name: Deploy
          
